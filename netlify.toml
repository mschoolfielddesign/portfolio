[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Required for React Router client-side routes (e.g. /case-study/medable-agent-studio)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
